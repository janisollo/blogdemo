<%- include('partials/header'); -%>
    <h1>Home</h1>
    <p>
        <%= startingContent %>

            <%= console.log(posts); %>

                <% for (var i=0; i<posts.length; i++) { %>
                    <% console.log(posts[i].title)%>
                        <% }%>
    </p>
    <%- include('partials/footer'); -%>